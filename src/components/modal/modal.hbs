<div class='modal__overflow' style={{#if isOpen}}"display:flex;"{{else}}'display:none;'{{/if}}>
  <div class='modal__wrapper'>
    <div class="modal__header">
      <h2 class='modal__title'>{{title}}</h2>
      {{{closeButton}}}
    </div>
    <div class='modal__content'>{{{form}}}</div>
  </div>
</div>